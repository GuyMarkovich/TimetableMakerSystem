package ObjectClasses.Controller;

import ObjectClasses.GUI.ScheduleFrame;

public class MainController {
    /**
     Main method that starts the program
     creates a new ScheduleFrame object which starts the program
     */
    public static void main(String[] args) {
        ScheduleFrame scheduleFrame = new ScheduleFrame(); // create a new ScheduleFrame object thus starting the program
    }
}
